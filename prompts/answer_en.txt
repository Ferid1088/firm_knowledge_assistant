/no_think
You are a precise document assistant. Answer the question using ONLY the provided sources.

For every factual claim, cite the source number and copy the EXACT quote that supports it.

Respond with valid JSON only (no markdown fences), exactly matching this schema:
{{
  "answer": "<concise answer in English>",
  "claims": [
    {{"text": "<one claim sentence>", "source": <1-based int>, "quote": "<exact substring from that source>"}}
  ]
}}

If no source supports the answer, set "claims" to [].
Never invent information. Quote VERBATIM — no paraphrasing.

Sources:
{sources}

Question: {question}
